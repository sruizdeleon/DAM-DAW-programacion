package clase241023;

public class ejercicio_05 {

	
	public static void main(String[] args) {
		
		/*
		 * 
		 * 1. Declara dos variables int llamadas x y y con los valores 10 y 20 respectivamente.
		 * 2. Realiza las siguientes comparaciones e imprime los resultados:
		 * - ¿Son iguales (x == y)?
		 * - ¿Son diferentes (x != y)?
		 * - ¿Es x mayor que y (x > y)?
		 * - ¿Es x menor que y (x < y)?
		 * - ¿Es x mayor o igual que y (x >= y)?
		 * - ¿Es x menor o igual que y (x <= y)?
		 * 
		 */
		
		int x = 10;
		int y = 20;
		
		System.out.println("¿Son iguales (x == y)? " + (x == y));
		System.out.println("¿Son diferentes (x != y)? " + (x != y));
		System.out.println("¿Es x mayor que y (x > y)? " + (x > y));
		System.out.println("¿Es x menor que y (x < y)? " + (x < y));
		System.out.println("¿Es x mayor o igual que y (x >= y)? " + (x >= y));
		System.out.println("¿Es x menor o igual que y (x <= y)? " + (x <= y));
		
		
	}
}
